[session_id: 6a45ba0a36ad58be43f6c2ef | topic_summary_time: 2026-07-02 09:21:42]User addressed issues with the OwlStudy project, including inaccurate face detection and AI service errors. Fixed 'AsyncClient proxies' error by modifying chat_service.py and embedding.py to handle proxy parameters compatibly. Enhanced face detection in vision.py by increasing YOLOv8 resolution to 416, adjusting confidence thresholds, and adding pupil detection/analysis. Implemented logic linking pupil dilation (pupil_ratio > 0.38) and gaze deviation to user distraction. Updated all training scripts (train_yolov8_face.py, train_yolov8_eye.py, etc.) to use 500 epochs with AdamW optimizer and enhanced data augmentation. --- User requested training YOLOv8 models for 500 epochs to detect faces and facial features (e.g., pupil dilation) for inferring learning states like distraction. Training scripts now support command-line parameters for epochs, device, and batch size. Current status: AI service errors fixed, face detection improved with pupil/gaze analysis, training scripts updated; models ready for 500-epoch training.
[session_id: 6a45ba0a36ad58be43f6c2ef | topic_summary_time: 2026-07-02 09:51:56]User reported ongoing AI conversation bugs, specifically the 'AsyncClient.init() got an unexpected keyword argument 'proxies'' error. Root cause identified as remaining OpenAI library dependency in the embedding module, which still read system proxy variables. Fixed by completely removing OpenAI library and using direct HTTPX calls for both DeepSeek chat and embedding APIs. Updated embedding.py, chat_service.py, config.py, .env, and .env.example to reflect DeepSeek-only configuration. All AI functionality now exclusively uses DeepSeek API with HTTPX client to avoid proxy conflicts. User needs to restart backend service for changes to take effect.
[session_id: 6a45ba0a36ad58be43f6c2ef | topic_summary_time: 2026-07-02 10:33:18]User reported that the AI service error 'AsyncClient.init() got an unexpected keyword argument 'proxies'' persisted after restarting the backend. The assistant identified that the HTTPX client might still be reading system proxy environment variables, leading to the error. A fix was implemented by adding trust_env=False to HTTPX client initialization in chat_service.py and embedding.py to force ignoring proxy settings, along with maintaining existing proxy environment variable clearing in main.py. The user was instructed to restart the backend and verify code version via http://127.0.0.1:8000/api/chat/debug/info.
[session_id: 6a45ba0a36ad58be43f6c2ef | topic_summary_time: 2026-07-02 15:04:58]User requested to change the OpenAI API to DeepSeek API in the settings (where the DeepSeek API Key was already configured) and fix the bug preventing question answering. The error message indicated an unexpected 'proxies' keyword argument in AsyncClient.init(). The solution involved ensuring the backend uses DeepSeek API exclusively with HTTPX client and trust_env=False to ignore system proxy variables, as previously established in project conventions.
[session_id: 6a45ba0a36ad58be43f6c2ef | topic_summary_time: 2026-07-02 15:19:56]User requested fixing two issues: replacing OpenAI API with DeepSeek API and resolving the bug preventing question answering in the application. The root cause of the answering bug was identified in chat.py line 85, where an async generator function generate_answer_stream was incorrectly called with await, causing a TypeError. This was fixed by removing the await and using async for directly. Additionally, OpenAI settings were removed from owlstudy-app.html, DeepSeek API was unified in chat_service.py using httpx, the API key was configured in .env, and SQLite compatibility was fixed in rag_service.py by replacing ILIKE with LOWER(content) LIKE LOWER(...). Both /api/chat/send (non-streaming) and /api/chat/stream (streaming) endpoints now return 200 with normal AI responses.
