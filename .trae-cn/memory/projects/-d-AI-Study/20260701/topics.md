[session_id: 6a44811c55ee4047adb0314c | topic_summary_time: 2026-07-01 14:40:05]User reported a network error in their project, initially thought to be CORS-related. The assistant modified CORS settings in `ai-tutor-backend/app/config.py` (setting `CORS_ORIGINS` to `["*"]`) and `app/main.py` (setting `allow_credentials` to `False`), but the issue persisted. Further investigation revealed the backend wasn't starting due to a `settings` naming conflict in `app/main.py` and missing/corrupted dependencies (`pydantic-core`, `jiter`, `greenlet`). These were fixed, dependencies added to `requirements.txt`, and backend successfully restarted. The `/api/health` endpoint and `/api/auth/register` interface now work, resolving the frontend network error. User is instructed to use `start-all.bat` for future restarts and `Ctrl + F5` if old errors persist.
